# Server configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# Notes storage configuration
# If integrating a real database in future, supply NOTES_DB_URL accordingly.
# For current file-based storage, you can customize the JSON file path:
# NOTES_DB_URL=
NOTES_STORAGE_FILE=./data/notes.json
